
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welcome To Swt Store Jailbreak Ps4 9.60</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background: linear-gradient(to bottom right, #063FAB, #01258A);
      background-image: url('icon0.jpg');
      background-size: cover;
      background-position: center;
      color: white;
      font-family: Arial, sans-serif;
      height: 100vh;
      overflow: hidden;
      position: relative;
    }

    @font-face {
      font-family: 'logging';
      src: url('fonts/LiberationMono-Regular.ttf');
    }

    .card {
      position: absolute;
      bottom: 30px;
      left: 20px;
      right: 20px;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(10px);
      padding: 40px;
      border-radius: 20px;
      text-align: center;
      width: 90%;
      max-width: 700px;
      box-shadow: 0 0 20px rgba(0,0,0,0.3);

    }

    h1 {
      font-size: 28px;
      margin-bottom: 15px;
    }

    #console {
      font-family: 'logging', monospace;
      font-size: 16px;
      text-align: left;
      background: rgba(0, 0, 0, 0.7);
      padding: 15px;
      border-radius: 10px;
      white-space: pre-wrap;
      height: 200px;
      overflow-y: auto;
    }

    #clock {
      position: absolute;
      top: 18px;
      right: 2.25%;
      font-weight: bold;
      font-size: 28px;
      color: white;
    }

    #date {
      position: absolute;
      top: 18px;
      left: 1.25%;
      font-weight: bold;
      font-size: 28px;
      color: white;
    }

    @media (max-width: 600px) {
      .card {
        padding: 20px;
        border-radius: 15px;
      }

      h1 {
        font-size: 20px;
      }

      #console {
        font-size: 14px;
        height: 150px;
      }

      #clock, #date {
        font-size: 20px;
      }
    }
  </style>

  <script>
    function showdate() {
        var dt = new Date();
        var Hour = dt.getHours();
        var Minute = dt.getMinutes();
        var Day = dt.getDate();
        var Month = dt.getMonth() + 1;
        var Year = dt.getFullYear();

        if (Minute < 10) { Minute = "0" + Minute; };
        if (Hour < 10) { Hour = "0" + Hour; };

        document.getElementById("date").innerHTML = (Day + "/" + Month + "/" + Year);
        document.getElementById("clock").innerHTML = (Hour + ":" + Minute);
    }

    setInterval(showdate, 1000);
  </script>
</head>

<body>
  <span id="date"></span>
  <span id="clock"></span>

  <div class="card">
    <h1>Swt Store Jailbreak for PS4 Firmware 9.60 No Usb Needed</h1>
    <pre id="console"></pre>
  </div>

  <script src="payload.js"></script>
  <script type="module" src="./alert.mjs"></script>
</body>
</html>
